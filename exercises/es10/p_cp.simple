var x: int, y: int, z: int;
begin
    x = 10;
    y = 0;
    z = 0;
    while x > 0 do
        x = x - 1;
        if brandom then
            if brandom then
                y = y + 1;
            else
                z = z + 1;
            endif;
        endif;
    done;
end
