var x: int, y: int, z: int;
begin
    x = 0;
    y = 0;
    z = 0;
    while x + y + z < 10 do
        if brandom then
            x = x + 1;
        else if brandom then
            y = y + 1;
        else if brandom then
            z = z + 1;
        endif;endif;endif;
    done;
end
